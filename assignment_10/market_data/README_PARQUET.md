Parquet files are not generated in this environment because the required engines (pyarrow/fastparquet) are not installed.
On your local machine, install pyarrow and run parquet_storage.py to
generate partitioned Parquet files under this directory.
